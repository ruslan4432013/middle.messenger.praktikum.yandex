<div class="{{ field_wrapper }}">
  <p class="{{ label_text }}">{{ label }}</p>
  <input
    {{#if onlyRead}}disabled{{/if}}
    {{#if fieldType}}type="{{fieldType}}"{{/if}}
          class="{{ value_text }}"
          value="{{ value }}"
  >
</div>
